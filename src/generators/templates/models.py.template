"""SQLAlchemy ORM models para ${app_name}."""
from sqlalchemy import Column, Integer, String, Float, Boolean, DateTime, UUID, ForeignKey
from sqlalchemy.sql import func
from database import Base
from uuid import uuid4

${models}
