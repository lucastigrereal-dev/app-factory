{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "ScaffoldPlan",
  "type": "object",
  "description": "Schema for the scaffold plan generated by the App Factory.  This draft defines the expected structure of the plan that describes the file tree and configuration required for scaffolding a new application.",
  "properties": {
    "plan_id": { "type": "string" },
    "blueprint_id": { "type": "string" },
    "file_tree": {
      "type": "object",
      "description": "Nested object representing the directory/file structure.  Keys are directory or file names; values are either nested objects (for directories) or metadata objects (for files)."
    },
    "configuration": { "type": "object" },
    "generated_at": { "type": "string", "format": "date-time" }
  },
  "required": [ "plan_id", "blueprint_id", "file_tree", "configuration", "generated_at" ]
}